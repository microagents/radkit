{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Built-in Tool Provider",
  "description": "Configuration for a single built-in tool.",
  "type": "object",
  "required": ["type", "name"],
  "properties": {
    "type": {
      "const": "builtin"
    },
    "name": {
      "type": "string",
      "enum": ["update_status", "save_artifact"],
      "description": "The name of the built-in tool to enable."
    }
  },
  "additionalProperties": false
}
